{
  "name": "snailcolony-next",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "dev": "pnpm --filter @snail/shared build && concurrently -k \"pnpm --filter @snail/server dev\" \"pnpm --filter @snail/client dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.3"
  }
}
