{
  "name": "@snail/client",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @snail/shared build && vite",
    "build": "pnpm --filter @snail/shared build && vite build",
    "preview": "vite preview",
    "lint": "pnpm --filter @snail/shared build && tsc --noEmit -p tsconfig.json",
    "test": "pnpm --filter @snail/shared build && vitest run --passWithNoTests"
  },
  "dependencies": {
    "@snail/shared": "workspace:*",
    "colyseus.js": "^0.15.26",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
